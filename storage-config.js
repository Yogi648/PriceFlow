/* Public storage API configuration.
   Set only a public endpoint and, if your API uses one, a public client token.
   Never put a database password, service-role key, or cloud secret here.
*/
window.PRICEFLOW_STORAGE_API_URL =
  'https://priceflow-storage-api.yogi648-priceflow.workers.dev';

window.PRICEFLOW_STORAGE_API_KEY =
  'PriceFlowPublicClient-2026';
